import { layout, widgets, editMode } from './ui';
import { recentlyOpened } from './workbench';

export const $ui = {
  layout,
  widgets,
  editMode,
};

export const $workbench = {
  recentlyOpened,
};
